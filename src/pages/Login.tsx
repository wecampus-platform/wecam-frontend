import React from 'react';
import LoginForm from '../components/LoginForm';

type Props = {
  
};

const Login = ({}: Props) => {
  return (
    <div>
        <LoginForm />
    </div>
  );
};

export default Login;
